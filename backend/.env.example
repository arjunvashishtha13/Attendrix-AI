PORT=3000
CLIENT_URL=http://localhost:3001
MONGODB_URI=mongodb://127.0.0.1:27017/attendrix
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
FACE_CONFIDENCE_THRESHOLD=0.72

# Optional SMTP for email reminders
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Attendrix AI <noreply@attendrix.ai>

# Optional OAuth (Channeli)
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:3000/api/v1/auth/oauth/callback
OAUTH_PROVIDER_URL=https://channeli.in
